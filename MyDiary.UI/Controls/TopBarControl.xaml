<UserControl x:Class="MyDiary.UI.Controls.TopBarControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="800">
    <Border Background="{StaticResource Brush.Surface}" BorderBrush="{StaticResource Brush.Border}" BorderThickness="0,0,0,1">
        <Grid Margin="16,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="Записи" Style="{StaticResource GhostButton}" Height="44" FontSize="14" Click="EntriesButton_Click" Margin="0,0,10,0" />
            <Button Grid.Column="1" Content="Статистика" Style="{StaticResource GhostButton}" Height="44" FontSize="14" Click="StatisticsButton_Click" Margin="0,0,10,0" />
            <Button Grid.Column="2" Content="Настройки" Style="{StaticResource GhostButton}" Height="44" FontSize="14" Click="SettingsButton_Click" />
        </Grid>
    </Border>
</UserControl>
