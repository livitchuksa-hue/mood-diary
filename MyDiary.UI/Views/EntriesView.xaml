<UserControl x:Class="MyDiary.UI.Views.EntriesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="{StaticResource Brush.Background}">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="Записи" FontSize="22" FontWeight="SemiBold" />
                <TextBlock Text="Календарь и карточки записей" Foreground="{StaticResource Brush.TextMuted}" />
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top">
                <Button x:Name="PrevMonthButton" Content="◀" Width="44" Style="{StaticResource GhostButton}" Margin="0,0,10,0" Click="PrevMonthButton_Click" />
                <TextBlock x:Name="MonthText" Text="" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{StaticResource Brush.TextMuted}" />
                <Button x:Name="NextMonthButton" Content="▶" Width="44" Style="{StaticResource GhostButton}" Click="NextMonthButton_Click" />
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Background="{StaticResource Brush.Surface}" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" CornerRadius="16" Padding="18">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock Text="Календарь" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10" />

                    <Border Grid.Row="1" Background="{StaticResource Brush.Surface2}" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" CornerRadius="14" Padding="12">
                        <Viewbox Stretch="Uniform">
                            <UniformGrid x:Name="CalendarDaysGrid" Columns="7" Rows="5" Width="420" Height="300" />
                        </Viewbox>
                    </Border>
                </Grid>
            </Border>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Border Background="{StaticResource Brush.Surface}" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" CornerRadius="16" Padding="18" Margin="0,0,0,12">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <TextBlock Text="Карточки записей" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10" />

                        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                            <ComboBox x:Name="MoodFilterCombo" Width="170" Margin="0,0,10,0" SelectionChanged="MoodFilterCombo_SelectionChanged" />
                            <ComboBox x:Name="ActivityFilterCombo" Width="170" SelectionChanged="ActivityFilterCombo_SelectionChanged" />
                        </StackPanel>

                        <Border Grid.Row="2" Background="{StaticResource Brush.Surface2}" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" CornerRadius="14" Padding="10">
                            <Grid>
                                <ScrollViewer x:Name="EntriesScrollViewer" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                    <ItemsControl x:Name="EntriesList">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Button Style="{StaticResource GhostButton}" BorderThickness="0" Padding="0" Margin="0,0,0,10" Click="EntryButton_Click" Tag="{Binding}" HorizontalContentAlignment="Stretch">
                                                    <Border Background="{StaticResource Brush.Surface}" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" CornerRadius="14" Padding="14" Height="120" HorizontalAlignment="Stretch">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="Auto" />
                                                                <ColumnDefinition Width="12" />
                                                                <ColumnDefinition Width="*" />
                                                                <ColumnDefinition Width="Auto" />
                                                            </Grid.ColumnDefinitions>

                                                            <Border Width="44" Height="44" CornerRadius="22" Background="{Binding MoodStatus, Converter={StaticResource MoodStatusToBrushConverter}}" VerticalAlignment="Top">
                                                                <TextBlock Text="{Binding Mood}" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold" />
                                                            </Border>

                                                            <StackPanel Grid.Column="2">
                                                                <TextBlock Text="{Binding Title}" FontSize="16" FontWeight="SemiBold" />
                                                                <TextBlock Text="{Binding Summary}" Foreground="{StaticResource Brush.TextMuted}" TextTrimming="CharacterEllipsis" MaxHeight="42" TextWrapping="Wrap" />
                                                                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                                                    <ItemsControl ItemsSource="{Binding Activities}">
                                                                        <ItemsControl.ItemsPanel>
                                                                            <ItemsPanelTemplate>
                                                                                <StackPanel Orientation="Horizontal" />
                                                                            </ItemsPanelTemplate>
                                                                        </ItemsControl.ItemsPanel>
                                                                        <ItemsControl.ItemTemplate>
                                                                            <DataTemplate>
                                                                                <Border Background="#14000000" CornerRadius="10" Padding="8,4" Margin="0,0,8,0">
                                                                                    <TextBlock Text="{Binding}" Foreground="{StaticResource Brush.TextMuted}" />
                                                                                </Border>
                                                                            </DataTemplate>
                                                                        </ItemsControl.ItemTemplate>
                                                                    </ItemsControl>
                                                                </StackPanel>
                                                            </StackPanel>

                                                            <TextBlock Grid.Column="3" Text="{Binding CreatedAt, StringFormat={}{0:dd.MM HH:mm}}" Foreground="{StaticResource Brush.TextMuted}" VerticalAlignment="Top" />
                                                        </Grid>
                                                    </Border>
                                                </Button>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>

                                <Border x:Name="EntriesEmptyStatePanel" Background="{StaticResource Brush.Surface2}" Visibility="Collapsed">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="340">
                                        <TextBlock Text="Сейчас здесь пусто" FontSize="18" FontWeight="SemiBold" TextAlignment="Center" />
                                        <TextBlock Text="Тут будут ваши записи. Чтобы добавить запись, нажмите кнопку «Создать запись»." Margin="0,8,0,0" Foreground="{StaticResource Brush.TextMuted}" TextWrapping="Wrap" TextAlignment="Center" />
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Border>
                    </Grid>
                </Border>

                <Button Grid.Row="1" Content="Создать запись" FontSize="24" Style="{StaticResource PrimaryButton}" Height="88" Click="CreateEntryButton_Click" />
            </Grid>
        </Grid>
    </Grid>
</UserControl>
