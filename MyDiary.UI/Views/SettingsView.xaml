<UserControl x:Class="MyDiary.UI.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="{StaticResource Brush.Background}">
    <Grid Margin="22">
        <Border Background="{StaticResource Brush.Surface}" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" CornerRadius="16" Padding="18">
            <StackPanel>
                <TextBlock Text="Настройки" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,8" />
                <TextBlock Text="Экран будет описан позже. Сейчас это заглушка." Foreground="{StaticResource Brush.TextMuted}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
