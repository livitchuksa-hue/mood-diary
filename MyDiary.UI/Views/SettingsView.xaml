<UserControl x:Class="MyDiary.UI.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="{StaticResource Brush.Background}">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,16">
            <TextBlock Text="Настройки" FontSize="22" FontWeight="SemiBold" />
            <TextBlock Text="Пока реализован только интерфейс" Foreground="{StaticResource Brush.TextMuted}" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Background="{StaticResource Brush.Surface}" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" CornerRadius="16" Padding="18">
                <StackPanel>
                    <TextBlock Text="Язык" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10" />
                    <TextBlock Text="Выбери язык интерфейса (реальную смену языка подключишь через конвертер)" Foreground="{StaticResource Brush.TextMuted}" TextWrapping="Wrap" Margin="0,0,0,10" />
                    <ComboBox x:Name="LanguageCombo" Height="44" SelectionChanged="LanguageCombo_SelectionChanged">
                        <ComboBoxItem Content="Русский" IsSelected="True" />
                        <ComboBoxItem Content="English" />
                    </ComboBox>

                    <TextBlock Text="Кастомная активность" FontSize="16" FontWeight="SemiBold" Margin="0,18,0,10" />
                    <TextBlock Text="Добавь свою активность (пока только UI)" Foreground="{StaticResource Brush.TextMuted}" Margin="0,0,0,10" />

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="12" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                    </Grid>

                    <Button Content="Создать активность" Style="{StaticResource PrimaryButton}" Margin="0,12,0,0" Click="CreateActivityButton_Click" />

                    <TextBlock x:Name="ActivityHint" Text="" Foreground="{StaticResource Brush.TextMuted}" Margin="0,10,0,0" />
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="{StaticResource Brush.Surface}" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" CornerRadius="16" Padding="18">
                <StackPanel>
                    <TextBlock Text="Аккаунт" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10" />
                    <TextBlock Text="Выход выполнит возврат на экран авторизации" Foreground="{StaticResource Brush.TextMuted}" TextWrapping="Wrap" Margin="0,0,0,10" />
                    <Button Content="Выйти из аккаунта" Style="{StaticResource PrimaryButton}" Click="LogoutButton_Click" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
