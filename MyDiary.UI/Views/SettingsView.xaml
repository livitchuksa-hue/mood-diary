<UserControl x:Class="MyDiary.UI.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="{StaticResource Brush.Background}">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,16">
            <TextBlock Text="Настройки" FontSize="22" FontWeight="SemiBold" />
            <TextBlock Text="Пока реализован только интерфейс" Foreground="{StaticResource Brush.TextMuted}" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Background="{StaticResource Brush.Surface}" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" CornerRadius="16" Padding="18">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="1" Margin="0,0,0,0">
                        <TextBlock Text="Тема" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10" />
                        <TextBlock Text="Выбери оформление приложения и нажми «Применить»" Foreground="{StaticResource Brush.TextMuted}" TextWrapping="Wrap" Margin="0,0,0,10" />
                        <ComboBox x:Name="ThemeCombo" Height="44">
                            <ComboBoxItem Content="Светлая" />
                            <ComboBoxItem Content="Тёмная" />
                        </ComboBox>
                        <Button Content="Применить" Style="{StaticResource PrimaryButton}" Margin="0,12,0,0" Click="ApplyThemeButton_Click" Canvas.ZIndex="1"/>
                        <TextBlock x:Name ="InfoMessageText" Text="" Foreground="{StaticResource Brush.TextMuted}" Margin="0,-18,0,0" Canvas.ZIndex="0"/>
                    </StackPanel>

                    <StackPanel Grid.Row="2" Margin="0,18,0,0">
                        <TextBlock Text="Кастомная активность" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10" />
                        <TextBlock Text="Добавь свою активность" Foreground="{StaticResource Brush.TextMuted}" Margin="0,0,0,10" />

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="12" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                        </Grid>

                        <Button Content="Создать активность" Style="{StaticResource PrimaryButton}" Margin="0,12,0,0" Click="CreateActivityButton_Click" />
                    </StackPanel>

                    <TextBlock Grid.Row="3" Text="Активности" FontSize="16" FontWeight="SemiBold" Margin="0,18,0,10" />

                    <Border Grid.Row="4" Background="{StaticResource Brush.Surface2}" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" CornerRadius="14" Padding="12">
                        <Grid>
                            <ScrollViewer x:Name="ActivitiesScrollViewer" VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Disabled">
                                <ItemsControl x:Name="ActivitiesList">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <UniformGrid Columns="2" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Button Style="{StaticResource GhostButton}" BorderThickness="0" Padding="0" Margin="0,0,12,12" Click="ActivityCard_Click" Tag="{Binding}" HorizontalContentAlignment="Stretch">
                                                <Border Background="{StaticResource Brush.Surface}" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" CornerRadius="14" Padding="14" Height="120" HorizontalAlignment="Stretch">
                                                    <Grid>
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="*" />
                                                        </Grid.RowDefinitions>

                                                        <TextBlock Text="{Binding Title}" FontSize="16" FontWeight="SemiBold" />

                                                        <TextBlock Grid.Row="1" Text="{Binding Description}" Foreground="{StaticResource Brush.TextMuted}" TextTrimming="CharacterEllipsis" TextWrapping="Wrap" MaxHeight="44" />
                                                    </Grid>
                                                </Border>
                                            </Button>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>

                            <Border x:Name="ActivitiesEmptyStatePanel" Background="{StaticResource Brush.Surface2}" Visibility="Collapsed">
                                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="380">
                                    <TextBlock Text="Пока тут пусто" FontSize="18" FontWeight="SemiBold" TextAlignment="Center" />
                                    <TextBlock Text="Здесь будут ваши активности. Чтобы добавить активность, нажмите кнопку «Создать активность»." Margin="0,8,0,0" Foreground="{StaticResource Brush.TextMuted}" TextWrapping="Wrap" TextAlignment="Center" />
                                </StackPanel>
                            </Border>
                        </Grid>
                    </Border>
                </Grid>
            </Border>

            <Border Grid.Column="2" Background="{StaticResource Brush.Surface}" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" CornerRadius="16" Padding="18">
                <StackPanel>
                    <TextBlock x:Name="UserName" Text="Аккаунт" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10" />
                    <TextBlock Text="Выход выполнит возврат на экран авторизации" Foreground="{StaticResource Brush.TextMuted}" TextWrapping="Wrap" Margin="0,0,0,10" />
                    <Button Content="Выйти из аккаунта" Style="{StaticResource PrimaryButton}" Click="LogoutButton_Click" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
