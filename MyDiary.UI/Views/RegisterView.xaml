<UserControl x:Class="MyDiary.UI.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="{StaticResource Brush.Background}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="560" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="1" Background="{StaticResource Brush.Surface}" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" CornerRadius="16" Padding="26" VerticalAlignment="Center">
            <StackPanel>
                <TextBlock Text="Регистрация" FontSize="28" FontWeight="SemiBold" Margin="0,0,0,8" />
                <TextBlock Text="Поля пока без проверок — кнопка просто ведёт дальше." Foreground="{StaticResource Brush.TextMuted}" Margin="0,0,0,18" />

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="12" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Логин" Foreground="{StaticResource Brush.TextMuted}" Margin="0,0,0,6" />
                        <TextBox Height="40" Margin="0,0,0,12" />

                        <TextBlock Text="Имя" Foreground="{StaticResource Brush.TextMuted}" Margin="0,0,0,6" />
                        <TextBox Height="40" Margin="0,0,0,12" />
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Пароль" Foreground="{StaticResource Brush.TextMuted}" Margin="0,0,0,6" />
                        <PasswordBox Height="40" Margin="0,0,0,12" />

                        <TextBlock Text="Повтор пароля" Foreground="{StaticResource Brush.TextMuted}" Margin="0,0,0,6" />
                        <PasswordBox Height="40" Margin="0,0,0,12" />
                    </StackPanel>
                </Grid>

                <Button Content="Зарегистрироваться" Style="{StaticResource PrimaryButton}" Click="RegisterButton_Click" />
                <Button Content="Назад" Style="{StaticResource GhostButton}" Margin="0,10,0,0" Click="BackButton_Click" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
