<UserControl x:Class="MyDiary.UI.Views.PaymentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="{StaticResource Brush.Background}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="640" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="1" Background="{StaticResource Brush.Surface}" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" CornerRadius="16" Padding="26" VerticalAlignment="Center">
            <StackPanel>
                <TextBlock Text="Оплата подписки" FontSize="28" FontWeight="SemiBold" Margin="0,0,0,8" />

                <TextBlock Text="К оплате" Foreground="{StaticResource Brush.TextMuted}" Margin="0,0,0,6" />

                <Border Background="{StaticResource Brush.Surface2}" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" CornerRadius="14" Padding="16" Margin="0,0,0,18">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock x:Name="PlanTitleText" Text="Месяц" FontSize="18" FontWeight="SemiBold" />
                        </StackPanel>
                        <TextBlock x:Name="PlanPriceText" Grid.Column="1" Text="320 ₽" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center" />
                    </Grid>
                </Border>

                <TextBlock Text="Данные карты" Foreground="{StaticResource Brush.TextMuted}" Margin="0,0,0,6" />
                
                <TextBlock Text="Номер карты" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8" />
                <TextBox x:Name="CardNumberBox" Height="44" Margin="0,0,0,14" MaxLength="16"/>

                <Grid Margin="0,0,0,18">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="110" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="110" />
                        <ColumnDefinition Width="16" />
                        <ColumnDefinition Width="160" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Text="Срок действия" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8" />
                    <TextBlock Grid.Row="0" Grid.Column="4" Text="Код" FontSize="14" FontWeight="SemiBold" Margin="50,0,-153,8" />

                    <TextBox x:Name="CardExpiryMonthBox" Grid.Row="1" Grid.Column="0" Height="44" HorizontalContentAlignment="Center" MaxLength="2"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="/" VerticalAlignment="Center" Foreground="{StaticResource Brush.TextMuted}" FontSize="18" Margin="10,0" />
                    <TextBox x:Name="CardExpiryYearBox" Grid.Row="1" Grid.Column="2" Height="44" HorizontalContentAlignment="Center" MaxLength="2"/>
                    <TextBox x:Name="CardCvcBox" Grid.Row="1" Grid.Column="4" Height="44" HorizontalContentAlignment="Center" Margin="50,0,-153,0" MaxLength="3"/>
                </Grid>
                <TextBlock x:Name ="ErrorMessageText" Text="" Foreground="{StaticResource Brush.TextMuted}" Margin="0,-18,0,0"/>
                <Button Content="Оплатить" Style="{StaticResource PrimaryButton}" Click="PayButton_Click" />
                <Button Content="Назад" Style="{StaticResource GhostButton}" Margin="0,10,0,0" Click="BackButton_Click" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
