<UserControl x:Class="MyDiary.UI.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="{StaticResource Brush.Background}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="520" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="1" Background="{StaticResource Brush.Surface}" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" CornerRadius="16" Padding="26" VerticalAlignment="Center">
            <StackPanel>
                <TextBlock Text="MyDiary" FontSize="28" FontWeight="SemiBold" Margin="0,0,0,8" />
                <TextBlock Text="Дневник настроения" Foreground="{StaticResource Brush.TextMuted}" Margin="0,0,0,18" />

                <TextBlock Text="Логин" Foreground="{StaticResource Brush.TextMuted}" Margin="0,0,0,6" />
                <TextBox x:Name="LoginTextBox" Height="40" Margin="0,0,0,12" />

                <TextBlock Text="Пароль" Foreground="{StaticResource Brush.TextMuted}" Margin="0,0,0,6" />
                <PasswordBox x:Name="PasswordBox" Height="40" Margin="0,0,0,18" />

                <Button Content="Войти" Style="{StaticResource PrimaryButton}" Click="LoginButton_Click" />
                <Button Content="Регистрация" Style="{StaticResource GhostButton}" Margin="0,10,0,0" Click="GoToRegisterButton_Click" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
