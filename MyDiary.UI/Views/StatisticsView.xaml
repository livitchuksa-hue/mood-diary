<UserControl x:Class="MyDiary.UI.Views.StatisticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="{StaticResource Brush.Background}">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="Статистика" FontSize="22" FontWeight="SemiBold" />
                <TextBlock Text="Выбери период и режим агрегации" Foreground="{StaticResource Brush.TextMuted}" />
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top">
                <DatePicker x:Name="StartDatePicker" Width="150" Margin="0,0,10,0" />
                <DatePicker x:Name="EndDatePicker" Width="150" Margin="0,0,18,0" />

                <Border Background="{StaticResource Brush.Surface}" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" CornerRadius="12" Padding="6">
                    <StackPanel Orientation="Horizontal">
                        <RadioButton x:Name="DayModeRadio" GroupName="Agg" Content="День" IsChecked="True" Margin="8,0" Foreground="{StaticResource Brush.Text}" Checked="ModeRadio_Checked" />
                        <RadioButton x:Name="WeekModeRadio" GroupName="Agg" Content="Неделя" Margin="8,0" Foreground="{StaticResource Brush.Text}" Checked="ModeRadio_Checked" />
                        <RadioButton x:Name="MonthModeRadio" GroupName="Agg" Content="Месяц" Margin="8,0" Foreground="{StaticResource Brush.Text}" Checked="ModeRadio_Checked" />
                    </StackPanel>
                </Border>

                <Button x:Name="ApplyButton" Content="Применить" Style="{StaticResource PrimaryButton}" Margin="18,0,0,0" Click="ApplyButton_Click" />
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Background="{StaticResource Brush.Surface}" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" CornerRadius="16" Padding="18">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="3*" />
                        <RowDefinition Height="2*" />
                    </Grid.RowDefinitions>
                    <TextBlock Text="График настроения" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10" />

                    <TextBlock x:Name="LineLegendText" Grid.Row="1" Text="" Foreground="{StaticResource Brush.TextMuted}" Margin="0,0,0,10" />

                    <Border Grid.Row="2" Background="{StaticResource Brush.Surface2}" CornerRadius="14" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" Margin="0,0,0,14" MinHeight="260" ClipToBounds="True">
                        <Canvas x:Name="LineChartCanvas" />
                    </Border>

                    <Border Grid.Row="3" Background="{StaticResource Brush.Surface2}" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" CornerRadius="14" Padding="16" MinHeight="220">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <TextBlock Text="Итоги периода" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,12" />

                            <UniformGrid Grid.Row="1" Columns="2" Rows="2">
                                <Border Background="{StaticResource Brush.Surface}" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" CornerRadius="12" Padding="16" Margin="0,0,12,12">
                                    <StackPanel>
                                        <TextBlock Text="Среднее" Foreground="{StaticResource Brush.TextMuted}" FontSize="13" />
                                        <TextBlock x:Name="KpiAvgMoodText" Text="—" FontSize="22" FontWeight="SemiBold" />
                                    </StackPanel>
                                </Border>

                                <Border Background="{StaticResource Brush.Surface}" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" CornerRadius="12" Padding="16" Margin="0,0,0,12">
                                    <StackPanel>
                                        <TextBlock Text="Тренд" Foreground="{StaticResource Brush.TextMuted}" FontSize="13" />
                                        <TextBlock x:Name="KpiTrendText" Text="—" FontSize="22" FontWeight="SemiBold" />
                                    </StackPanel>
                                </Border>

                                <Border Background="{StaticResource Brush.Surface}" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" CornerRadius="12" Padding="16" Margin="0,0,12,0">
                                    <StackPanel>
                                        <TextBlock Text="Мин/Макс" Foreground="{StaticResource Brush.TextMuted}" FontSize="13" />
                                        <TextBlock x:Name="KpiMinMaxText" Text="—" FontSize="22" FontWeight="SemiBold" />
                                    </StackPanel>
                                </Border>

                                <Border Background="{StaticResource Brush.Surface}" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" CornerRadius="12" Padding="16">
                                    <StackPanel>
                                        <TextBlock Text="Точек" Foreground="{StaticResource Brush.TextMuted}" FontSize="13" />
                                        <TextBlock x:Name="KpiPointsText" Text="—" FontSize="22" FontWeight="SemiBold" />
                                    </StackPanel>
                                </Border>
                            </UniformGrid>
                        </Grid>
                    </Border>
                </Grid>
            </Border>

            <Grid Grid.Column="2" Margin="0,0,0,33">
                <Grid.RowDefinitions>
                    <RowDefinition Height="3*" />
                    <RowDefinition Height="2*" />
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="{StaticResource Brush.Surface}" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" CornerRadius="16" Padding="18" Margin="0,0,0,16" MinHeight="320">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <TextBlock Text="Эмоции за месяц" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10" />

                        <Border Grid.Row="1" Background="{StaticResource Brush.Surface2}" CornerRadius="14" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" Padding="12" Margin="0,0,0,12">
                            <Grid Width="220" Height="220" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Canvas x:Name="PieChartCanvas" Width="220" Height="220" />
                                <Ellipse Fill="{StaticResource Brush.Surface2}" Width="130" Height="130" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                <TextBlock x:Name="PieCenterText" Text="" Foreground="{StaticResource Brush.TextMuted}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Grid>
                        </Border>

                        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                            <StackPanel x:Name="PieLegendPanel" />
                        </ScrollViewer>
                    </Grid>
                </Border>

                <Border Grid.Row="1" Background="{StaticResource Brush.Surface}" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" CornerRadius="16" Padding="18" MinHeight="220">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <TextBlock Text="Краткое описание" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10" />
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                            <TextBlock Text="Пока это заглушка. Здесь будет текст: какие эмоции встречаются чаще, динамика по неделям, и т.д." TextWrapping="Wrap" Foreground="{StaticResource Brush.TextMuted}" />
                        </ScrollViewer>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
