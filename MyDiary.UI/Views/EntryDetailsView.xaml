<UserControl x:Class="MyDiary.UI.Views.EntryDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="{StaticResource Brush.Background}">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
            <Border Width="44" Height="44" CornerRadius="22" Background="{StaticResource Brush.Accent}" Margin="0,0,12,0" />
            <StackPanel>
                <TextBlock x:Name="TitleText" Text="Запись" FontSize="22" FontWeight="SemiBold" />
                <TextBlock x:Name="MetaText" Text="" Foreground="{StaticResource Brush.TextMuted}" />
            </StackPanel>
        </StackPanel>

        <Border Grid.Row="1" Background="{StaticResource Brush.Surface}" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" CornerRadius="16" Padding="18">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="ContentText" TextWrapping="Wrap" FontSize="14" />
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Назад" Style="{StaticResource GhostButton}" Margin="0,0,10,0" Click="BackButton_Click" />
            <Button Content="Изменить" Style="{StaticResource GhostButton}" Margin="0,0,10,0" Click="EditButton_Click" />
            <Button Content="Удалить" Style="{StaticResource PrimaryButton}" Click="DeleteButton_Click" />
        </StackPanel>
    </Grid>
</UserControl>
