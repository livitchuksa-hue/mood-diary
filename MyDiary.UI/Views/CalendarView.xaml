<UserControl x:Class="MyDiary.UI.Views.CalendarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="{StaticResource Brush.Background}">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="Календарь" FontSize="22" FontWeight="SemiBold" />
                <TextBlock x:Name="PeriodText" Text="" Foreground="{StaticResource Brush.TextMuted}" />
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top">
                <Button x:Name="PrevButton" Content="◀" Width="44" Style="{StaticResource GhostButton}" Margin="0,0,10,0" Click="PrevButton_Click" />
                <Button x:Name="NextButton" Content="▶" Width="44" Style="{StaticResource GhostButton}" Click="NextButton_Click" />
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Background="{StaticResource Brush.Surface}" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" CornerRadius="16" Padding="18">
            <Border Background="{StaticResource Brush.Surface2}" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" CornerRadius="14" Padding="12">
                <Viewbox Stretch="Uniform">
                    <UniformGrid x:Name="DaysGrid" Columns="7" Rows="5" Width="420" Height="300" />
                </Viewbox>
            </Border>
        </Border>
    </Grid>
</UserControl>
