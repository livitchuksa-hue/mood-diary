<UserControl x:Class="MyDiary.UI.Views.CalendarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="{StaticResource Brush.Background}">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="Календарь" FontSize="22" FontWeight="SemiBold" />
                <TextBlock x:Name="PeriodText" Text="" Foreground="{StaticResource Brush.TextMuted}" />
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top">
                <Button x:Name="PrevButton" Content="◀" Width="44" Style="{StaticResource GhostButton}" Margin="0,0,10,0" Click="PrevButton_Click" />
                <Button x:Name="NextButton" Content="▶" Width="44" Style="{StaticResource GhostButton}" Margin="0,0,18,0" Click="NextButton_Click" />

                <Border Background="{StaticResource Brush.Surface}" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" CornerRadius="12" Padding="6">
                    <StackPanel Orientation="Horizontal">
                        <RadioButton x:Name="DaysModeRadio" Content="Дни" IsChecked="True" Margin="8,0" Checked="ModeRadio_Checked" Foreground="{StaticResource Brush.Text}" />
                        <RadioButton x:Name="WeeksModeRadio" Content="Недели" Margin="8,0" Checked="ModeRadio_Checked" Foreground="{StaticResource Brush.Text}" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Background="{StaticResource Brush.Surface}" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" CornerRadius="16" Padding="18">
            <Grid>
                <UniformGrid x:Name="DaysGrid" Columns="7" />
                <UniformGrid x:Name="WeeksGrid" Columns="13" Visibility="Collapsed" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
